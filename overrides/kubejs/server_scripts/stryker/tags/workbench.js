// priority: 0
//TODO: we need to use the data folder to add tags, tags should not be checked from within the tags event
onEvent('item.tags', event => {
	//ids = Ingredient.of('#forge:workbenches').itemIds
	//
	//ids.forEach(id => {
	//	event.add('forge:workbench', id)
	//})
})